services:
  analysis-env:
    build: 
      context: .
      dockerfile: Dockerfile
    image: testing
    ports:
      - "8787:8787"
    volumes:
      - .:/home/rstudio/work
    environment:
      PASSWORD: "password"
      DISABLE_AUTH: true

      